[package]
name = "smart_term"
version = "0.1.0"
edition = "2024"

[dependencies]
crossterm = "0.29.0"
dirs = "7.0.0"
portable-pty = "0.9.0"
rusqlite = { version = "0.40.2", features = ["bundled"] }

[profile.release]
opt-level = 3 
lto = true 
codegen-units = 1 
strip = true 
panic = "abort"